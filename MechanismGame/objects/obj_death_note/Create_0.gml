dialogString = "Queerly shaped shard embedded in the prostrated body, its elliptical surface suggests containing usage.";

box_width = sprite_get_width(spr_text_box);
stringHeight = string_height(dialogString);

drawText = false;
